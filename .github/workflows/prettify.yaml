---
name: Run prettier on Marion's code

on:
  push:

permissions:
  contents: write

jobs:
  prettier-fix:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Invoke the Prettier fix
        uses: WorkOfStan/prettier-fix@main
        with:
          commit-changes: true
